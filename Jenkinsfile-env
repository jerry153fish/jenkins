pipeline {
    agent any 

    environment {
        DB_ENGINE    = 'sqlite'
		Jenkins_PSW = credentials('github-jerry')
    }

    stages {
        stage('Build') {
            steps {
                echo "DISABLE_AUTH is ${DISABLE_AUTH}"
                echo "Jenkins password is ${Jenkins_PSW}"
            }
        }
    }
}
