pipeline {
    agent any 

    environment {
        DB_ENGINE    = 'sqlite'
		GITHUB_CRED = credentials('github-jerry')
    }

    stages {
        stage('Build') {
            steps {
				sh 'scripts/env_print.sh' 
            }
        }
    }
}
